import React from 'react'; 
// Import React to enable JSX rendering

function Profile() {
  return (
    <div>
      <h2>Your Profile</h2> {/* Heading for the profile section */}
      <p>This is a sample profile page using React Router.</p> {/* Description or placeholder content */}
    </div>
  );
}

export default Profile; 
// Export the Profile component so it can be used in other parts of the app (e.g., in App.js)
